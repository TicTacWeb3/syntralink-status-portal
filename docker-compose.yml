services:
  syntralink-status-portal:
    image: ghcr.io/tictacweb3/syntralink-status-portal:latest
    ports:
      - "3000:3000"
    volumes:
      - syntralink-db:/data
    restart: unless-stopped

volumes:
  syntralink-db:
